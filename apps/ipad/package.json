{
  "name": "@logicsim/ipad",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:web": "pnpm --filter @logicsim/web build",
    "sync": "pnpm run build:web && cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/filesystem": "^7.0.0",
    "@capacitor/share": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  }
}
